my_string = input("Input some text that you'd like to get the length of:")

print(len(my_string))






# Lists are used to store a collection of variables. It comes in handy for when we wish to group more significant
# amounts of data. We’ve used quite a few lists already in this course. Strings are lists that are composed of characters,
# or char. Char is a type that is never comprised of more than a single letter or number. When we group many of these chars,
# we end up with a string, which is a type of list. Because a string is a list, there are many other things that we can do with
# it other than just use string methods. Firstly, we can get the length of a string by using the len() function. Let’s try that out
# below: